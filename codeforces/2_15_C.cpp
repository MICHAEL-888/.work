//
// Created by Michael on 2024/2/15.
//
#include <bits/stdc++.h>

#define endl '\n'
using namespace std;

void solve() {
    int k,x,a;
    cin >> k >> x >> a;

}

signed main(void) {
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);

    int t = 1;
    cin >> t;

    while (t--) {
        solve();
    }

    return 0;
}